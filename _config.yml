remote_theme: chrisrhymes/bulma-clean-theme
livereload: true
defaults:
  -
    scope:
      path: "weeklynote/*" # an empty string here means all files in the project
    values:
      show_sidebar: false
      menubar: menu
